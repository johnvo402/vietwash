services:
  database:
    ports:
      - "5432:5432"
    volumes:
      - ./.pgdump:/backup:ro
  redis:
    ports:
      - "6379:6379"
  pgadmin:
    ports:
      - "5050:80"

volumes:
  nuget_cache:
  pgadmin_data:
