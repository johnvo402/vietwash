services:
  gateway:
    ports:
      - "127.0.0.1:5000:8080"
  database:
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - ./.pgdump:/backup:ro
  redis:
    ports:
      - "127.0.0.1:6379:6379"
  pgadmin:
    image: dpage/pgadmin4:9.17
    ports:
      - "127.0.0.1:5050:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
      PGADMIN_DEFAULT_SERVER: database
    depends_on:
      - database
    volumes:
      - pgadmin_data:/var/lib/pgadmin/

volumes:
  nuget_cache:
  pgadmin_data:
