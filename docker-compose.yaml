services:
  auth:
    container_name: auth
    image: auth
    ports:
      - "8080"
    restart: always
  ecommerce:
    container_name: ecommerce
    image: ecommerce
    ports:
      - "8080"
    restart: always
  project:
    container_name: project
    image: project
    ports:
      - "8080"
    restart: always

  gateway:
    container_name: gateway
    image: gateway
    ports:
      - "8080"
    restart: always

  finance:
    container_name: finance
    image: finance
    ports:
      - "8080"
    restart: always
